
const directive = {
    attributeId: ':',       // Character prepended to attribute to id it for this directive
    // Function that strips out template directives and returns processing objects
    preprocess: (element, attr) => {

    },
    // Processes the preprocessed template before attaching to the component
    process: (element, def) => {

    },
};


export const directives = {
    
}